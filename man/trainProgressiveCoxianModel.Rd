% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trainProgressiveCoxianModel.R
\name{trainProgressiveCoxianModel}
\alias{trainProgressiveCoxianModel}
\title{Train Progressive Coxian Model}
\usage{
trainProgressiveCoxianModel(
  training_data,
  n_phases,
  strata_by,
  time,
  event,
  cluster_by = NULL
)
}
\arguments{
\item{training_data}{A dataframe containing the training data, which must include a time-to-event variable (`time`), an event indicator (`event`),
and any covariates for stratification.}

\item{n_phases}{An integer specifying the total number of phases in the Coxian model.}

\item{strata_by}{A string specifying the column name in the training_data used for stratification (e.g., "phase").}

\item{time}{A string specifying the column name in the training_data that represents the current time variable.}

\item{event}{A string specifying the column name in the training_data that represents the event indicator (1 if event occurred, 0 if censored).}

\item{cluster_by}{An optional string specifying the column name in the training_data to be used for clustering.}
}
\value{
A list containing the results of the fitted Cox PH models for each phase.
}
\description{
This function trains a Progressive Coxian phase-type model using the provided training data.
It fits a Cox proportional hazards model and estimates transition rates for the specified number of phases.
}
\examples{
\dontrun{
  training_data <- data.frame(
    time = c(5, 10, 15, 20, 25),
    event = c(1, 0, 1, 1, 0),
    phase = c(1, 1, 2, 2, 3),
    cluster_id = c(101, 101, 102, 102, 103)
  )
  result <- trainProgressiveCoxianModel(training_data, n_phases = 3, strata_by = "phase", time = "time", event = "event", cluster_by = "cluster_id")
  print(result)
}

}
