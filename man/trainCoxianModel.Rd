% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trainCoxianModel.R
\name{trainCoxianModel}
\alias{trainCoxianModel}
\title{Train Coxian Model}
\usage{
trainCoxianModel(training_data, n_phases, strata_by)
}
\arguments{
\item{training_data}{A dataframe containing the training data, which must include time-to-event, event indicator,
and any covariates for stratification.}

\item{n_phases}{An integer specifying the total number of phases in the Coxian model.}

\item{strata_by}{A string specifying the column name in the training_data used for stratification.}
}
\value{
A list containing two elements: `fit_result`, which is the result of fitting the Cox PH model.
}
\description{
This function trains a Coxian phase-type model using the provided training data.
It fits a Cox proportional hazards model and estimates transition rates for the specified number of phases.
}
\examples{
\dontrun{
  training_data <- data.frame(
    time = c(5, 10, 15),
    status = c(1, 0, 1),
    phase = c(1, 2, 3)
  )
  result <- trainCoxianModel(training_data, n_phases = 3, strata_by = "phase")
  print(result)
}

}
